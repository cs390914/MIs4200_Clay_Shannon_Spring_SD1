@model IEnumerable<MIs4200_Clay_Shannon_Spring_SD1.Models.TrainingSessions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Client.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TrainingSessionDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Client.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TrainingSessionDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TrainingSessionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TrainingSessionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TrainingSessionID })
        </td>
    </tr>
}

</table>
